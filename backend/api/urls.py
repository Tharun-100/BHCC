from django.urls import path
from rest_framework_simplejwt.views import TokenRefreshView

from . import clinical_views, views

urlpatterns = [
    path("auth/login", views.auth_login),
    path("auth/login/", views.auth_login),
    path("auth/staff/request-otp", views.auth_staff_request_otp),
    path("auth/staff/request-otp/", views.auth_staff_request_otp),
    path("auth/staff/verify-otp", views.auth_staff_verify_otp),
    path("auth/staff/verify-otp/", views.auth_staff_verify_otp),
    path("auth/register-patient", views.auth_register_patient),
    path("auth/register-patient/", views.auth_register_patient),
    path("auth/me", views.auth_me),
    path("auth/me/", views.auth_me),
    path("auth/password-reset", views.auth_password_reset),
    path("auth/password-reset/", views.auth_password_reset),
    path("auth/password-reset/confirm", views.auth_password_reset_confirm),
    path("auth/password-reset/confirm/", views.auth_password_reset_confirm),
    path("auth/verify-email", views.auth_verify_email),
    path("auth/verify-email/", views.auth_verify_email),
    path("auth/resend-verification", views.auth_resend_verification),
    path("auth/resend-verification/", views.auth_resend_verification),
    path("auth/change-password", views.auth_change_password),
    path("auth/change-password/", views.auth_change_password),
    path("auth/token/refresh", TokenRefreshView.as_view()),
    path("auth/token/refresh/", TokenRefreshView.as_view()),
    path("patient/profile", views.patient_profile),
    path("patient/profile/", views.patient_profile),
    path("staff/profile", views.staff_profile),
    path("staff/profile/", views.staff_profile),
    path("departments", views.departments),
    path("departments/", views.departments),
    path("departments/<int:pk>", views.department_detail),
    path("departments/<int:pk>/", views.department_detail),
    path("doctors", views.doctors),
    path("doctors/", views.doctors),
    path("appointments", views.appointments),
    path("appointments/", views.appointments),
    path("appointments/<int:pk>", views.appointment_detail),
    path("appointments/<int:pk>/", views.appointment_detail),
    path("availability", views.availability),
    path("availability/", views.availability),
    path("feedback", views.feedback),
    path("feedback/", views.feedback),
    path("registrations", views.registrations),
    path("registrations/", views.registrations),
    path("admin/create-doctor", views.admin_create_doctor),
    path("admin/create-doctor/", views.admin_create_doctor),
    path("admin/doctors/<int:pk>/schedule", views.admin_update_doctor_schedule),
    path("admin/doctors/<int:pk>/schedule/", views.admin_update_doctor_schedule),
    path("admin/create-staff", views.admin_create_staff),
    path("admin/create-staff/", views.admin_create_staff),
    path("admin/accounts/<int:pk>/", views.admin_update_account),
    path("admin/accounts/", views.admin_all_accounts),
    path("admin/staff-accounts/", views.admin_staff_accounts),
    path("admin/dashboard", views.admin_dashboard),
    path("admin/dashboard/", views.admin_dashboard),
    # Browser-facing aliases. Some privacy/security extensions block requests
    # containing an /admin/ path even though these endpoints use JWT + IsAdmin.
    path("management/create-doctor/", views.admin_create_doctor),
    path("management/doctors/<int:pk>/schedule/", views.admin_update_doctor_schedule),
    path("management/create-staff/", views.admin_create_staff),
    path("management/accounts/<int:pk>/", views.admin_update_account),
    path("management/accounts/", views.admin_all_accounts),
    path("management/staff-accounts/", views.admin_staff_accounts),
    path("management/dashboard/", views.admin_dashboard),
    path("contact", views.contact),
    path("contact/", views.contact),
    path("payments/create-order", views.payments_create_order),
    path("payments/create-order/", views.payments_create_order),
    path("payments/verify", views.payments_verify),
    path("payments/verify/", views.payments_verify),
    path("attendance", clinical_views.attendance),
    path("attendance/", clinical_views.attendance),
    path("leave-requests", clinical_views.leave_requests),
    path("leave-requests/", clinical_views.leave_requests),
    path("admin/attendance", clinical_views.admin_attendance),
    path("admin/attendance/", clinical_views.admin_attendance),
    path("admin/attendance/<int:pk>", clinical_views.admin_attendance_detail),
    path("admin/attendance/<int:pk>/", clinical_views.admin_attendance_detail),
    path("admin/leave-requests", clinical_views.admin_leave_requests),
    path("admin/leave-requests/", clinical_views.admin_leave_requests),
    path("admin/leave-requests/<int:pk>", clinical_views.admin_leave_detail),
    path("admin/leave-requests/<int:pk>/", clinical_views.admin_leave_detail),
    path("management/attendance/", clinical_views.admin_attendance),
    path("management/attendance/<int:pk>/", clinical_views.admin_attendance_detail),
    path("management/leave-requests/", clinical_views.admin_leave_requests),
    path("management/leave-requests/<int:pk>/", clinical_views.admin_leave_detail),
    path("appointments/<int:appointment_id>/prescription", clinical_views.appointment_prescription),
    path("appointments/<int:appointment_id>/prescription/", clinical_views.appointment_prescription),
    path("prescriptions", clinical_views.prescriptions),
    path("prescriptions/", clinical_views.prescriptions),
    path("prescriptions/<int:pk>/finalize", clinical_views.finalize_prescription),
    path("prescriptions/<int:pk>/finalize/", clinical_views.finalize_prescription),
    path("prescriptions/<int:pk>/amend", clinical_views.amend_prescription),
    path("prescriptions/<int:pk>/amend/", clinical_views.amend_prescription),
    path("prescriptions/<int:pk>/pdf", clinical_views.prescription_pdf),
    path("prescriptions/<int:pk>/pdf/", clinical_views.prescription_pdf),
]
